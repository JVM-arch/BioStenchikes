# 🚀 Инструкция по деплою для jvm-arch.github.io

## Ваш сайт будет доступен по адресу:
**https://jvm-arch.github.io**

## Шаги для деплоя:

### 1. Создайте репозиторий на GitHub

Репозиторий **ОБЯЗАТЕЛЬНО** должен называться: **`jvm-arch.github.io`**

1. Перейдите на [github.com](https://github.com)
2. Нажмите "New repository"
3. Название репозитория: **`jvm-arch.github.io`** (точно так!)
4. Сделайте репозиторий **Public** (или Private с GitHub Pro)
5. **НЕ** добавляйте README, .gitignore или лицензию
6. Нажмите "Create repository"

### 2. Загрузите код

```bash
# Если git еще не инициализирован
git init

# Добавьте все файлы
git add .

# Создайте первый коммит
git commit -m "Initial commit: Bio profile with admin panel"

# Переименуйте ветку в main (если нужно)
git branch -M main

# Добавьте remote (замените jvm-arch на ваш username, если отличается)
git remote add origin https://github.com/jvm-arch/jvm-arch.github.io.git

# Загрузите код
git push -u origin main
```

### 3. Включите GitHub Pages

1. Перейдите в ваш репозиторий: `https://github.com/jvm-arch/jvm-arch.github.io`
2. Откройте **Settings** (вверху справа)
3. В левом меню найдите **Pages**
4. В разделе **Source** выберите:
   - **Source**: `GitHub Actions`
5. Сохраните изменения

### 4. Дождитесь деплоя

1. После push GitHub Actions автоматически запустится
2. Перейдите во вкладку **Actions** в вашем репозитории
3. Дождитесь завершения workflow "Deploy to GitHub Pages" (обычно 1-2 минуты)
4. Когда увидите зеленую галочку ✅ - деплой успешен!

### 5. Проверьте сайт

Откройте в браузере: **https://jvm-arch.github.io**

## ✅ Готово!

Ваш сайт задеплоен! 🎉

### Доступ к админ-панели:

- **Основная админка**: Нажмите `Ctrl+Alt+A` на сайте или кликните точку в левом нижнем углу
- **Полная админ-панель**: https://jvm-arch.github.io/#/admin/profile
- **Пароль по умолчанию**: `admin123`

## 🔄 Обновление сайта

После каждого `git push` в ветку `main` сайт автоматически обновится через 1-2 минуты.

```bash
# Внесли изменения
git add .
git commit -m "Update profile"
git push
# Через 1-2 минуты сайт обновится автоматически!
```

## ⚙️ Настройка Remove.bg API (опционально)

Если хотите использовать функцию удаления фона:

1. Зарегистрируйтесь на [remove.bg/api](https://www.remove.bg/api)
2. Получите бесплатный API ключ (50 запросов/месяц)
3. В репозитории GitHub:
   - Settings → Secrets and variables → Actions
   - New repository secret
   - Name: `VITE_REMOVE_BG_API_KEY`
   - Value: ваш API ключ
   - Add secret
4. Обновите workflow (уже настроено в `.github/workflows/deploy.yml`)

## 🆘 Решение проблем

### Сайт показывает 404
- Убедитесь, что репозиторий называется точно `jvm-arch.github.io`
- Проверьте, что GitHub Pages включен (Settings → Pages)
- Подождите 2-3 минуты после первого деплоя

### Стили не загружаются
- Проверьте, что файл `.nojekyll` создан (уже есть в `public/`)
- Очистите кэш браузера (Ctrl+Shift+R)

### Админ-панель не открывается
- Используйте hash routing: `https://jvm-arch.github.io/#/admin/profile`
- Не используйте обычный путь `/admin/profile`

### GitHub Actions не запускается
- Проверьте, что файл `.github/workflows/deploy.yml` существует
- Убедитесь, что вы push'ите в ветку `main` или `master`

## 📞 Полезные ссылки

- Ваш сайт: https://jvm-arch.github.io
- Админ-панель: https://jvm-arch.github.io/#/admin/profile
- GitHub Actions: https://github.com/jvm-arch/jvm-arch.github.io/actions
- GitHub Pages Settings: https://github.com/jvm-arch/jvm-arch.github.io/settings/pages

---

**Удачи с деплоем! 🚀**

