# 🎨 Полная Админ-Панель - Инструкция по установке

## Быстрый старт

### 1. Настройка Remove.bg API (для удаления фона)

1. Зарегистрируйтесь на https://www.remove.bg/api
2. Получите бесплатный API ключ (50 бесплатных запросов в месяц)
3. Создайте файл `.env` в корне проекта:
   ```
   VITE_REMOVE_BG_API_KEY=ваш_api_ключ_здесь
   ```

### 2. Запуск проекта

```bash
npm install
npm run dev
```

### 3. Доступ к админ-панели

**Вариант 1:** Перейдите по адресу:
```
http://localhost:5173/#/admin/profile
```

**Вариант 2:** Откройте основную админку (Ctrl+Alt+A) и нажмите кнопку "🎨 Open Full Admin Panel →"

## Структура админ-панели

### ✅ Реализованные вкладки:

1. **Appearance** - Внешний вид
   - Typography (шрифты, размеры, цвета)
   - Colors (цветовая палитра)
   - Effects (эффекты: sparkles, glow, parallax и т.д.)
   - Animations (анимации страницы)

2. **Media** - Медиа файлы
   - Banner (с удалением фона ✨)
   - Background (с настройками размера и эффектов)
   - Avatar (с декорациями и эффектами)
   - Favicon

3. **Box & Layout** - Блоки и макет
   - Box Settings (размеры, отступы, скругления)
   - Box Colors (цвета и тени)
   - Border (границы)
   - Layout & Alignment (выравнивание)

### 🚧 В разработке:

4. **Embeds** - Встраиваемые элементы (Discord, Spotify, YouTube и т.д.)
5. **Badges & Tags** - Бейджи и теги
6. **Links** - Социальные ссылки
7. **Tracks** - Музыкальные треки (с удалением фона с обложек ✨)
8. **Embed & Share** - Встраивание и шаринг профиля
9. **Reveal Screen** - Экран раскрытия
10. **Settings** - Настройки (курсор, метаданные, экспорт/импорт)

## Функция удаления фона ✨

После загрузки изображения (Banner, Background, Avatar, Track Cover) появляется кнопка **"✨ Remove Background"**.

**Как использовать:**
1. Загрузите изображение
2. Нажмите "✨ Remove Background"
3. Дождитесь обработки (обычно 2-5 секунд)
4. В модальном окне сравните оригинал и обработанное изображение
5. Нажмите "Use Processed Image" для применения

**Важно:** 
- Требуется API ключ от remove.bg
- Бесплатный план: 50 запросов в месяц
- Для продакшена рекомендуется использовать локальный сервер с rembg

## Сохранение данных

Все настройки автоматически сохраняются в `localStorage` с ключом `admin-profile`.

При следующем открытии админ-панели все настройки будут восстановлены.

## Дизайн

Админ-панель выполнена в стиле fakecrime.bio:
- Темная тема (#0f0f0f)
- Glass morphism эффекты
- Акцентный цвет #3a7bef
- Плавные анимации и переходы
- Адаптивный дизайн

## Мобильная версия

- Sidebar автоматически скрывается на мобильных устройствах
- Grid layout адаптируется под размер экрана
- Модальные окна занимают весь экран

## Troubleshooting

**Проблема:** Кнопка "Remove Background" не работает
**Решение:** Проверьте наличие `VITE_REMOVE_BG_API_KEY` в `.env` файле

**Проблема:** Админ-панель не открывается
**Решение:** Убедитесь, что используете правильный URL: `#/admin/profile`

**Проблема:** Настройки не сохраняются
**Решение:** Проверьте консоль браузера на наличие ошибок

## Дополнительная информация

См. `README_ADMIN.md` для подробной документации по API и структуре проекта.

