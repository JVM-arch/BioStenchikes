# 🔒 Настройка безопасности админ-панели

## ⚠️ ВАЖНО: Сначала настройте учетные данные!

### Шаг 1: Создайте файл .env

В корне проекта создайте файл `.env` (он уже в .gitignore):

```env
VITE_ADMIN_USERNAME=ваш_логин
VITE_ADMIN_PASSWORD=ваш_безопасный_пароль
```

**Пример:**
```env
VITE_ADMIN_USERNAME=admin
VITE_ADMIN_PASSWORD=MySecurePass123!
```

### Шаг 2: Для GitHub Pages (продакшн)

1. Перейдите в ваш репозиторий: `https://github.com/jvm-arch/jvm-arch.github.io`
2. Откройте **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **New repository secret**
4. Добавьте два секрета:

   **Секрет 1:**
   - Name: `VITE_ADMIN_USERNAME`
   - Value: ваш логин (например: `admin`)
   - Add secret

   **Секрет 2:**
   - Name: `VITE_ADMIN_PASSWORD`
   - Value: ваш пароль (например: `MySecurePass123!`)
   - Add secret

### Шаг 3: Пересоберите проект

После добавления секретов:

```bash
git add .
git commit -m "Add security configuration"
git push
```

GitHub Actions автоматически пересоберет проект с новыми учетными данными.

## ✅ Проверка

1. Откройте сайт: `https://jvm-arch.github.io`
2. Нажмите `Ctrl+Alt+A` или кликните точку в левом нижнем углу
3. Введите ваш логин и пароль
4. Должен открыться доступ к админ-панели

## 🔐 Безопасность

### Что защищено:

✅ **Пароли не хранятся в коде** - только в переменных окружения  
✅ **Хеширование паролей** - пароли сравниваются по хешам  
✅ **Сессии** - автоматический выход через 24 часа  
✅ **Обфускация кода** - код минифицирован и обфусцирован  
✅ **Source maps отключены** - нельзя просмотреть исходный код  
✅ **Console.log удалены** - нет отладочной информации  

### Защита от просмотра кода:

- Код минифицирован (terser)
- Имена переменных обфусцированы
- Файлы имеют хешированные имена
- Source maps отключены
- Console.log удалены

## 🆘 Если забыли пароль

1. Обновите секрет `VITE_ADMIN_PASSWORD` в GitHub Secrets
2. Пересоберите проект (push в main)
3. Очистите localStorage в браузере (F12 → Application → Local Storage → Clear)

## 📝 Дефолтные учетные данные

Если не настроить `.env` или секреты, используются дефолтные:
- Логин: `admin`
- Пароль: `admin123`

**⚠️ ВАЖНО:** Смените дефолтные учетные данные перед деплоем!

## 🔄 Смена пароля

1. Измените `VITE_ADMIN_PASSWORD` в `.env` (локально)
2. Обновите секрет `VITE_ADMIN_PASSWORD` в GitHub Secrets
3. Сделайте push - проект пересоберется автоматически

---

**Помните:** Безопасность зависит от вас! Храните учетные данные в секрете.

