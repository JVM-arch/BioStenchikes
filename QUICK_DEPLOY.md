# ⚡ Быстрый деплой на GitHub Pages

## Шаг 1: Создайте репозиторий на GitHub

1. Перейдите на [github.com](https://github.com)
2. Создайте новый репозиторий (например, `my-bio`)
3. **НЕ** инициализируйте с README, .gitignore или лицензией

## Шаг 2: Загрузите код

```bash
# Инициализируйте git (если еще не сделано)
git init

# Добавьте все файлы
git add .

# Создайте коммит
git commit -m "Initial commit"

# Переименуйте ветку в main (если нужно)
git branch -M main

# Добавьте remote (замените на ваш репозиторий)
git remote add origin https://github.com/ВАШ_USERNAME/НАЗВАНИЕ_РЕПОЗИТОРИЯ.git

# Загрузите код
git push -u origin main
```

## Шаг 3: Настройте GitHub Pages

1. Перейдите в ваш репозиторий на GitHub
2. Откройте **Settings** → **Pages**
3. В разделе **Source** выберите:
   - **Source**: `GitHub Actions`
4. Сохраните изменения

## Шаг 4: Настройте base path (если нужно)

Если ваш репозиторий **НЕ** называется `username.github.io`:

1. Откройте `vite.config.js`
2. Найдите функцию `getBasePath()`
3. В разделе "Для локальной разработки" раскомментируйте и укажите название репозитория:

```js
if (process.env.NODE_ENV === 'production') {
  return '/НАЗВАНИЕ_ВАШЕГО_РЕПОЗИТОРИЯ/'; // например: '/my-bio/'
  return '/';
}
```

4. Сохраните и закоммитьте:
```bash
git add vite.config.js
git commit -m "Update base path"
git push
```

## Шаг 5: Дождитесь деплоя

1. После push GitHub Actions автоматически запустится
2. Перейдите в **Actions** вкладку вашего репозитория
3. Дождитесь завершения workflow (обычно 1-2 минуты)
4. После успешного деплоя ваш сайт будет доступен по адресу:
   - `https://username.github.io/НАЗВАНИЕ_РЕПОЗИТОРИЯ`
   - Или `https://username.github.io` если репозиторий называется `username.github.io`

## ✅ Готово!

Ваш сайт задеплоен! 🎉

### Доступ к админ-панели:

- Основная: `https://ваш-сайт.github.io/#/admin/profile` (Ctrl+Alt+A)
- Полная: `https://ваш-сайт.github.io/#/admin/profile`

## 🔄 Обновление сайта

После каждого push в ветку `main` сайт автоматически обновится через 1-2 минуты.

## ⚠️ Важные замечания

1. **Base path**: Если репозиторий не `username.github.io`, обязательно укажите правильный base path
2. **API ключи**: Для remove.bg API добавьте секреты в GitHub Actions (Settings → Secrets)
3. **Роутинг**: Используйте hash routing (`#/admin/profile`) вместо обычного (`/admin/profile`)

## 🆘 Проблемы?

См. [DEPLOY.md](./DEPLOY.md) для подробной документации и troubleshooting.

