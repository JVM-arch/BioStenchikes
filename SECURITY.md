# 🔒 Безопасность админ-панели

## ⚠️ ВАЖНО: Настройка учетных данных

### Шаг 1: Создайте файл .env

Создайте файл `.env` в корне проекта (он уже в .gitignore, не будет закоммичен):

```env
VITE_ADMIN_USERNAME=ваш_логин
VITE_ADMIN_PASSWORD=ваш_безопасный_пароль
```

### Шаг 2: Для GitHub Pages (продакшн)

Учетные данные нужно добавить в GitHub Secrets:

1. Перейдите в ваш репозиторий на GitHub
2. Settings → Secrets and variables → Actions
3. Добавьте два секрета:
   - `VITE_ADMIN_USERNAME` = ваш логин
   - `VITE_ADMIN_PASSWORD` = ваш пароль

### Шаг 3: Обновите workflow

Файл `.github/workflows/deploy.yml` уже настроен для использования секретов:

```yaml
- name: Build
  run: npm run build
  env:
    GITHUB_REPOSITORY: ${{ github.repository }}
    NODE_ENV: production
    VITE_ADMIN_USERNAME: ${{ secrets.VITE_ADMIN_USERNAME }}
    VITE_ADMIN_PASSWORD: ${{ secrets.VITE_ADMIN_PASSWORD }}
```

## 🛡️ Меры безопасности

### 1. Хеширование паролей
- Пароли не хранятся в открытом виде
- Используется простое хеширование для клиентской проверки
- Учетные данные сравниваются по хешам

### 2. Сессии
- Сессии действительны 24 часа
- Автоматический выход при истечении сессии
- Токены сессий хранятся в localStorage

### 3. Обфускация кода
- Минификация и обфускация в продакшн сборке
- Удаление console.log
- Обфускация имен переменных
- Хешированные имена файлов

### 4. Защита от просмотра кода
- Source maps отключены
- Код минифицирован
- Имена переменных обфусцированы
- Функции аутентификации скрыты

## 🔐 Рекомендации по паролям

- Используйте сложный пароль (минимум 12 символов)
- Комбинация букв, цифр и символов
- Не используйте простые пароли типа "admin123"
- Регулярно меняйте пароль

## ⚠️ Что НЕ делать

- ❌ НЕ коммитьте файл `.env` в git
- ❌ НЕ публикуйте учетные данные
- ❌ НЕ используйте простые пароли
- ❌ НЕ делитесь паролем с другими

## 🔄 Смена пароля

1. Измените `VITE_ADMIN_PASSWORD` в `.env` (локально)
2. Обновите секрет `VITE_ADMIN_PASSWORD` в GitHub Secrets
3. Пересоберите и задеплойте проект

## 🆘 Если пароль забыт

1. Очистите localStorage в браузере
2. Обновите секреты в GitHub
3. Пересоберите проект

---

**Помните:** Безопасность зависит от вас! Храните учетные данные в секрете.

