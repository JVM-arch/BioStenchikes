# 🚀 Деплой на GitHub Pages

## Автоматический деплой (рекомендуется)

### Шаг 1: Настройка репозитория

1. Создайте репозиторий на GitHub (если еще не создан)
2. Загрузите код в репозиторий:
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin https://github.com/ВАШ_USERNAME/НАЗВАНИЕ_РЕПОЗИТОРИЯ.git
   git push -u origin main
   ```

### Шаг 2: Включение GitHub Pages

1. Перейдите в Settings → Pages
2. В разделе "Source" выберите:
   - Source: **GitHub Actions**
3. Сохраните изменения

### Шаг 3: Автоматический деплой

После каждого push в ветку `main` или `master` проект автоматически соберется и задеплоится на GitHub Pages.

**URL вашего сайта:**
- Если репозиторий называется `username.github.io`: `https://username.github.io`
- Если репозиторий имеет другое название: `https://username.github.io/НАЗВАНИЕ_РЕПОЗИТОРИЯ`

## Ручной деплой

### Вариант 1: Через GitHub CLI

```bash
npm run build
gh-pages -d dist
```

### Вариант 2: Через git subtree

```bash
npm run build
git subtree push --prefix dist origin gh-pages
```

### Вариант 3: Через gh-pages пакет

1. Установите gh-pages:
   ```bash
   npm install --save-dev gh-pages
   ```

2. Добавьте в package.json:
   ```json
   "scripts": {
     "deploy": "npm run build && gh-pages -d dist"
   }
   ```

3. Задеплойте:
   ```bash
   npm run deploy
   ```

## Настройка base path

Если ваш репозиторий НЕ называется `username.github.io`, нужно обновить `vite.config.js`:

```js
export default defineConfig({
    base: '/НАЗВАНИЕ_ВАШЕГО_РЕПОЗИТОРИЯ/',
    // ...
})
```

Например, если репозиторий называется `my-bio`:
```js
base: '/my-bio/',
```

## Проверка деплоя

1. После деплоя подождите 1-2 минуты
2. Перейдите в Settings → Pages
3. Проверьте URL вашего сайта
4. Откройте сайт в браузере

## Доступ к админ-панели

После деплоя админ-панель будет доступна по адресу:
- `https://username.github.io/#/admin/profile`
- Или `https://username.github.io/НАЗВАНИЕ_РЕПОЗИТОРИЯ/#/admin/profile`

## Переменные окружения

Если вы используете Remove.bg API, создайте файл `.env`:

```
VITE_REMOVE_BG_API_KEY=your_api_key_here
```

**Важно:** GitHub Actions не имеет доступа к `.env` файлам из репозитория. Для продакшена:

1. Перейдите в Settings → Secrets and variables → Actions
2. Добавьте новый секрет: `VITE_REMOVE_BG_API_KEY`
3. Обновите `.github/workflows/deploy.yml`:

```yaml
- name: Build
  run: npm run build
  env:
    GITHUB_REPOSITORY: ${{ github.repository }}
    VITE_REMOVE_BG_API_KEY: ${{ secrets.VITE_REMOVE_BG_API_KEY }}
```

## Troubleshooting

### Проблема: Сайт показывает 404
**Решение:** Проверьте base path в `vite.config.js` - он должен соответствовать названию репозитория

### Проблема: Стили не загружаются
**Решение:** Убедитесь, что файл `.nojekyll` создан в папке `public/`

### Проблема: Роутинг не работает
**Решение:** GitHub Pages использует hash routing. Убедитесь, что используете `#/admin/profile` вместо `/admin/profile`

### Проблема: API ключи не работают
**Решение:** Добавьте секреты в GitHub Actions (см. раздел "Переменные окружения")

## Полезные ссылки

- [GitHub Pages Documentation](https://docs.github.com/en/pages)
- [Vite Deployment Guide](https://vitejs.dev/guide/static-deploy.html)
- [GitHub Actions Documentation](https://docs.github.com/en/actions)

